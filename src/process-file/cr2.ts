// const debug = require('./debug')('cr2');
// const exifDate = require('../exif-date');

module.exports = async (/* filePath, destFolder, ext, limit */) => {
  // const d = await exifDate(filePath);

  // debug('d=', d);
};
